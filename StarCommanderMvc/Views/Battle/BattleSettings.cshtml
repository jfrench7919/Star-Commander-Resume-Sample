@model StarCommanderMvc.Models.BattleSettings

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Battle Settings</h4>
        @Html.LabelFor(model => model.battleFieldType)<br />
        @Html.EnumDropDownListFor(model => model.battleFieldType)<br />
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    </div>
}
